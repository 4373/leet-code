/**
 * @param {string} A
 * @param {string} B
 * @return {number}
 */
var kSimilarity = function(A, B) {
  let same = 0
  const length = a.length
  for (let i = 0; i < length; i++) {
    if (a[i] === b[i]) same++
  }
}
